#!/bin/bash

clean
git commit -a -m "starting lab, committing changes"

echo Lab 1
git checkout -b lab1
git push -u

echo "This is a file" > file1
git add file1

git commit -a -m "lab1 commit1"

clear
git status
git branch -vv
